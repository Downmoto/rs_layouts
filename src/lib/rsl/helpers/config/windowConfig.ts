export interface WindowConfig {
    minWidth: number,
    minHeight: number,
    resizingZoneOverflow: number,
    snapOnByDefault: boolean,
    snapThreshold: number
}

// TODO: snap threshold and snappingOnByDefault